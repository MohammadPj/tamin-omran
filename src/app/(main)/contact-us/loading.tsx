import React from "react";
import {Container} from "@mui/material";

const ContactUsLoadingPage = () => {
  return (
    <Container sx={{ mt: 7, mb: 15, display: "flex", gap: 7 }}>

    </Container>
  );
};

export default ContactUsLoadingPage;
